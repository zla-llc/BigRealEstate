import { gteLength, gtLength, lteLength, ltLength } from "./length";
export { isValidString } from "./valid";
import { isValidString } from "./valid";

export const StringValidation = {
  gtLength,
  gteLength,
  ltLength,
  lteLength,
  isValidString,
};
