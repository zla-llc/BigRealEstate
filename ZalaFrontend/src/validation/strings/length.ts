export const gtLength = (value: string, length: number) =>
  value.length > length;

export const ltLength = (value: string, length: number) =>
  value.length < length;

export const gteLength = (value: string, length: number) =>
  value.length >= length;

export const lteLength = (value: string, length: number) =>
  value.length <= length;
