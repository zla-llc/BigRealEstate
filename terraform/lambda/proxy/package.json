{
  "scripts": {
    "build": "npx tsc",
    "postbuild": "cp package*.json dist && cd dist && npm i --omit=dev"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {}
}
